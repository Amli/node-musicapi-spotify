{
  "name": "node-musicapi-spotify",
  "version": "0.0.1",
  "description": "Spotify metadata API helper, with an attempt to unify the access to different metadata services",
  "main": "spotify.js",
  "scripts": {
    "test": "mocha --reporter spec --check-leaks",
    "coverage": "./node_modules/istanbul/lib/cli.js cover ./node_modules/mocha/bin/_mocha -- --ui bdd -R spec -t 5000"
  },
  "keywords": [
    "music",
    "api",
    "spotify"
  ],
  "author": "Emmanuel Magnier",
  "license": "MIT",
  "devDependencies": {
    "mocha": "~1.17.1",
    "istanbul": "~0.2.4"
  },
  "dependencies": {
    "request": "~2.33.0"
  }
}
